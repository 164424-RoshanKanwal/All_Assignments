package StreamPractice;

import java.util.Arrays;
import java.util.List;

public class Stream2 {

	List<String> list = Arrays.asList("Roshan","Rahul","oshfa ","heloo", "lall");
	
	List<String> modList = list.stream().filter(name-> name.startsWith("R")).collect(Collectors.toList);
}
